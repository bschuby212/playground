# Playground

Interactive portfolio playground — a freeform, draggable canvas of project thumbnails.

## Develop

```bash
npm install
npm run dev
```

## Configure

Edit `src/playground/data/config.ts` for hints, canvas size, scaling, and drag behavior.

Edit `src/playground/data/projects.ts` to replace placeholder titles, images, URLs, and positions.
