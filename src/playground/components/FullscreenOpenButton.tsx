/**
 * ≥1000px + Framer embed only: open the live Netlify/site URL as a natural
 * full page in a new tab (not as another embed).
 * Temporarily hidden — reposition later.
 */
export function FullscreenOpenButton() {
  return null
}
